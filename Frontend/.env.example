# Base URL of the Backend API (see ../Backend/server.js). No secrets live
# here — the Airtable key stays server-side in Backend/config/.env, never
# in this frontend.
VITE_API_BASE_URL=http://localhost:4000
